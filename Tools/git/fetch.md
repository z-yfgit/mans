## git fetch命令
说明：从另外一个仓库下载对象和引用

### 用法
```
git fetch [<options>] [<repository> [<refspec>...]]
git fetch [<options>] <group>
git fetch --multiple [<options>] [(<repository> | <group>)...]
git fetch --all [<options>]
```

| 选项 | 说明
| --- | ---
| --dry-run | 演习，不实际下载对象和引用
|  | 
|  | 

### 示例
```sh

```
