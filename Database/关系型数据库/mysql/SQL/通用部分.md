### LIKE模糊匹配
```sql
-- 后面模糊匹配
SELECT name FROM tbl_name WHERE name LIKE "hello%";

-- 前面模糊匹配
SHOW DATABASES LIKE "%schema";

-- 中间模糊匹配
SHOW VARIABLES LIKE "%slow_query%";
```

### ORDER BY


### LIMIT
LIMIT row_count
```sql
-- 只显示10行
SELECT * FROM table_name LIMIT 10;
```
