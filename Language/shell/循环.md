## 循环

### for循环
```sh
for num in {1..10}
do
    # 这里是循环体，可以添加你需要执行的操作
    echo "当前数字：$num"

done
```

### while循环
```sh
# 初始化循环变量
num=1

while true
do
    # 这里是循环体，可以添加你需要执行的操作
    echo "当前数字：$num"
    ((num++))
    if [ $num -gt 10 ]; then
        # 循环结束条件
        break
    fi
done
```
