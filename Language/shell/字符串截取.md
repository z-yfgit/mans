```sh
str="a.b.c.d"
echo "非贪婪：${str%.*}"    # 非贪婪：结果是a=a.b.c（删最后一个.及d）
echo "贪婪：${str%%.*}"     # 贪婪：结果b=a（删所有.及后面的内容，只留第一个.前的部分）
```

```sh
data="a b c d"
# 截取最后一个空格后的内容
echo "${data##* }"
```

```sh

${0%%.sh}

${filename%%.sh}	
${filename%*.sh}

${filename##*.}
${filename#*.}
```
